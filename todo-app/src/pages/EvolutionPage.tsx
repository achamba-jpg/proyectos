import { Box, Card, CardContent, Chip, Container, Grid, Typography } from '@mui/material';
import SectionHeader from '../components/SectionHeader';
import { EducationView } from '../types';
export default function EvolutionPage({ data }: { data: EducationView }) { return <Container maxWidth="lg" sx={{ py: 10 }}><SectionHeader eyebrow="01 · Evolución" title="Una especie capaz de adaptarse" text="La historia del Homo sapiens está marcada por la cooperación, la creatividad y la capacidad de aprender de cada generación." />{data.error && <Typography color="warning.main">{data.error}</Typography>}<Grid container spacing={3}>{data.evolucion.map((item) => <Grid item xs={12} md={4} key={item.id}><Card sx={{ height: '100%' }}><Box component="img" src={item.image} alt={item.title} sx={{ width: '100%', height: 230, objectFit: 'cover' }} /><CardContent><Chip label={item.period} size="small" color="secondary" /><Typography variant="h5" sx={{ mt: 2 }}>{item.title}</Typography><Typography color="text.secondary">{item.description}</Typography></CardContent></Card></Grid>)}</Grid></Container>; }
