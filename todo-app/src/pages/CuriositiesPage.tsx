import { Button, Card, CardContent, Chip, Container, Dialog, DialogActions, DialogContent, DialogTitle, Grid, Typography } from '@mui/material';
import { useState } from 'react';
import SectionHeader from '../components/SectionHeader';
import { EducationView } from '../types';
export default function CuriositiesPage({ data }: { data: EducationView }) { const [open, setOpen] = useState(false); return <Container maxWidth="lg" sx={{ py: 10 }}><SectionHeader eyebrow="04 · Curiosidades" title="Pequeñas historias, grandes descubrimientos" text="Conoce algunos datos que conectan el pasado con nuestra vida actual." /><Grid container spacing={3}>{data.curiosidades.map((item) => <Grid item xs={12} md={4} key={item.id}><Card sx={{ height: '100%' }}><CardContent><Chip label={item.category} color="secondary" size="small" /><Typography variant="h5" sx={{ mt: 2 }}>{item.title}</Typography><Typography color="text.secondary">{item.text}</Typography><Button sx={{ mt: 2 }} onClick={() => setOpen(true)}>Análisis GraphQL</Button></CardContent></Card></Grid>)}</Grid><Dialog open={open} onClose={() => setOpen(false)}><DialogTitle>GraphQL: análisis teórico</DialogTitle><DialogContent dividers><Typography paragraph>GraphQL no está implementado: la aplicación mantiene REST API.</Typography><Typography>El endpoint <strong>GET /api/evolucion</strong> podría beneficiarse de GraphQL si una vista necesitara al mismo tiempo periodo, imagen, características y herramientas relacionadas, solicitando solo los campos necesarios en una consulta.</Typography></DialogContent><DialogActions><Button onClick={() => setOpen(false)}>Cerrar</Button></DialogActions></Dialog></Container>; }
